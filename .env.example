# Google AI API Key for Gemini integration
GOOGLE_API_KEY=your_google_gemini_api_key

# Optional: Set to production for production builds
NODE_ENV=production

# Port configuration (default: 3000 for dev, 8008 for Docker)
PORT=3000
